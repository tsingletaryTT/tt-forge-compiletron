# TT-Forge Compiletron — application dependencies
#
# NOTE: torch, torchvision, transformers, numpy, jax, flax, etc. are
# provided by the forge environment (source ~/tt-forge-fe/env/activate).
# Do NOT pip-install them here — install this file AFTER activating forge.
#
# XLA backend (jax/pjrt) deps go in the separate xla-venv:
#   python3 -m venv xla-venv
#   xla-venv/bin/pip install pjrt-plugin-tt jax==0.7.1 jaxlib==0.7.1 \
#       flax==0.8.5 "transformers<5.0" torch \
#       --index-url https://pypi.tenstorrent.com/simple/

# TUI
textual>=0.59.0

# HuggingFace model discovery
huggingface_hub>=0.20.0

# ASCII banners
pyfiglet>=0.8.0

# Utilities
requests>=2.28.0
tqdm>=4.65.0

# Testing
pytest>=7.0.0
